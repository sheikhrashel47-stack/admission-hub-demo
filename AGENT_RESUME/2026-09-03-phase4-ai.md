# Resume — 2026-09-03 · 🤖 PHASE 4 AI ENGINE (v179)

## ভাইয়ের স্পেক (২৫ পয়েন্ট)
User-key লাগবে না; server-side credentials; current user auto-identify; global+user context engine;
question/vocab/mistake/exam/progress aware; strict isolation; per-user chat history; context memory;
speed; failure handling; API security; cost control; 4-layer prompt architecture; NO hallucination;
NO private data expose; AI UI unchanged; app intact; test matrix; APPROVAL GATE → "PHASE 4 APPROVED"।

## ভাই-এর দেওয়া
- Gemini key (AQ. ফরম্যাট — `/tmp/gkey`; শুধু CF secret-এ যাবে; কোনো ফাইলে/কমিটে নয়)
- AI চরিত্র: মাখন-ভাষা, মানুষের মতো (রোবটিক নয়), ১００% valid, একই কথা নয়, Rashel Zayan Sir (owner),
  অ্যাপ-উদ্দেশ্য-জ্ঞান, আপডেট তথ্য — সব SYS-লেয়ারে বসানো।

## কী করা হয়েছে (worker + client)
### worker-bundle.mjs
- SYS() নতুন: মাখন বাংলা/সততা-সর্বোচ্চ/হ্যালুসিনেশন-নিষিদ্ধ/অ্যাপ+owner/আপডেট-নোটিশ
- GEM_CHAIN = [gemini-3-flash-preview, gemini-3.1-flash-lite, gemini-2.5-flash] (2.5 new-key-তে বন্ধ)
- aiCall → নতুন AI CONTEXT ENGINE: aiBuildBrain/aiWorstTopics/aiTrend/aiFindQ/aiFindVocab
- ৪-লেয়ার: SYSTEM(ai SYS) + GLOBAL(pubContent: bank/sub/topic/vocab/adminInstruction) +
  USER(ustate: examResults/mistakes/dailyStats/activityLogs/notes/vocabulary — সঠিক keys!) + REQUEST
- exact question/vocab lookup (refs.questionId/word) — স্পেক ৬/৭
- image kind (gemini-3.1-flash-image) — একই secure পথ, ক্লায়েন্ট-কী নেই
- cache aicache:<uid>+kind+refs+lastMsg (টTL ৬০০s) · রেট-লিমিট ১২/মিনিট (ailim:<uid>:<minute>)
- chat history per-user `achat:<uid>` (cap ৪০) · timeout 26s AbortController · 429 বাংলা+retryAfter
- গেস্ট-allow: token ছাড়াও /api/ai (uid="" → শুধু global, user-data না) — isolation অটুট
- PERSONAL_KEYS + vocabulary (শেখা-শব্দ cloud-সিঙ্ক)
### client
- **ah-ai-client.js** (নতুন): window.AH_AI = {ask, askImage, tok, isAuthed, userId} — একমাত্র /api পথ;
  Bearer শুধু token থাকলে; image-AI-ও একই /ai kind=image
- **study-ai-tool.js**: gemini-ask → server-first (AH_AI.ask, kind chat); fail-এ friendly বাংলা (429 বিশেষ);
  image → server-first; chat owner-স্কোপ (ownerOf ফিল্টার) — স্পেক ১৩ (A/B আলাদা)
- **ai-explain-tool.js**: generate() → server-first kind=explain + refs.questionId; agent fallback
- **premium-auth.js**: PERSONAL + vocabulary
- index/sw: ah-ai-client.js?v=ahai-v1 · studyai-v133-server · aiex-v4-ph4 · **v179-aiengine-20260903**
- AUTH_ENDPOINTS_GUARD tag + aiengine

## Test
ai-phase4 24/24 · nosplash 16/16 · auth-ux 33/33 · session-survive 12/12 · GUARD PASS

## বাকি
1. CF secret `GEMINI_KEYS` put করা (wrangler auth লাগে — CLOUDFLARE_API_TOKEN চাই বা GH workflow-
   secret-এ থাকলে that path) — key /tmp/gkey
2. worker deploy (wrangler --keep-vars অথবা .github/workflows/main.yml dispatch)
3. live E2E (A/B ইউজার, isolation, chat-history, retry, বড়-ডেটা) — তারপর ভাই-এর টেস্ট
