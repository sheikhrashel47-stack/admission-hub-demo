# Resume — 2026-09-02 · Auth flow v33 (signup verify + passkey fix) — LIVE

## ভাইয়ের রিপোর্ট (রাগান্বিত)
"signup করতে গেলে Gmail verification এবং Passkey verification করতে গেলে বলতেছে আগে লগইন করো। মাথা গরম। পুরো লগইন সিস্টেম আগের মতো করে দাও। ১০০% fix না হওয়া পর্যন্ত থামবে না।"

## Root causes (২টি, ১০০% নিশ্চিত)
1. **signup verify স্ক্রিনে passkey বাটন ছিল** → verify স্ক্রিনে (লগইন session ছাড়া) passkey চাপলে `doPasskeyRegister` → `/auth/passkey/register/finish` worker-এ **auth লাগে** → 401 "আগে লগইন করো" error। Passkey register-এর জন্য আগে লগইন করা session জরুরি — signup-এ passkey বাটন থাকারই কথা নয়।
2. **verify স্ক্রিনের copy বিভ্রান্তিকর** — "অ্যাকাউন্ট যাচাইকরণ" + "Continue with Email Verification" + passkey বাটন এক স্ক্রিনে → ভাই একে "Gmail verification" মনে করছেন।

## ফিক্স v33 (commit `11af33f` — LIVE)
1. **signupVerify স্ক্রিন**: passkey বাটন + hint সরানো। এখন শুধু `📧 ইমেইলে কোড পাঠান` বাটন। → "আগে লগইন করো" আর আসবে না।
2. **Copy (bn+en)**:
   - verifyTitle: 'ইমেইল যাচাই করুন' / 'Verify your email'
   - verifySub: 'আপনার ইমেইলে ৬-অঙ্কের কোড পাঠানো হবে...' (6-digit code, কোনো link নয়)
   - emailWay: 'ইমেইলে কোড পাঠান' / 'Send code to my email'
3. **login-এ passkey fail** → NotFoundError হলে বাংলা: 'এই ডিভাইসে পাসকি পাওয়া যায়নি — ইমেইল/পাসওয়ার্ড বা গুগল দিয়ে ঢুকো' (আগে শুধু 'Passkey ব্যর্থ')।
4. **গুগল signup**: doGoogle-এ signup ফিল্ড (নাম/জন্মতারিখ/স্কুল/কলেজ) grab → গুগল দিয়ে অ্যাকাউন্ট খুললে ভাইয়ের তথ্য draft-এ থেকে worker-এ যায় (আগে 'Scholar' ডিফল্ট)।
5. SW BUILD_ID `v163-auth-fix-20260902`, APP_SHELL `p3-auth-prof-v33`।

## টেস্ট (jsdom /tmp/t/test.js) — ১১/১১ ✅
signup render → verify: email-way বাটন আছে, passkey **নেই** (bug fixed) → email form; login: passkey আছে → NotFoundError → 'পাসকি পাওয়া যায়নি' + 'ইমেইল/পাসওয়ার্ড' hint; google fail → fallback বাটন + help box।

## Live verify
- index `p3-auth-prof-v33`, sw `v163-auth-fix-20260902` (Pages build ~২ মিনিট)
- live premium-auth.js: signupVerify-তে `ahPasskey` **0** বার, `পাসকি পাওয়া যায়নি` ১, `ইমেইল যাচাই করুন` ১, `ইমেইলে কোড পাঠান` ১ ✅

## Git
- demo HEAD `11af33f` pushed. আগের টার্নে local HEAD দূষিত ছিল (d977a11) → `git reset --hard origin/main` (v32) → clean-এ ৪টি ছোট edit → commit v33।

## নোট: worker URL (সংশোধিত)
- সঠিক URL: `https://admission-gk.rashelzayan213.workers.dev` (subdomain `rashelzayan213`; আগের resume-এ ভুল `sheikhrashel47-stack` লেখা ছিল — client-এ সবসময় সঠিক ছিল)।
- Live verify (এই টার্ন, CF API + curl): deployment `48fc321d` 100% live; `/api/health` ok; `/api/auth/config` google/passkey/email true; `register-email` → `sent:true, channel:otp, expiresIn:120` ✅

## পরের ধাপ
- LATEST.md + resume push (demo + control sync)।
- ভাইকে বোঝানো: signup = ইমেইল + ৬-অঙ্কের কোড (আর passkey বাটন signup-এ নেই); passkey শুধু login-এ (আগে set করা থাকলে); passkey না থাকলে স্পষ্ট বাংলা বলবে।
- ভাইয়ের কনফার্মেশন; fail স্ক্রিনশট এলে screen-specific fix; worker DNS retry।
