# Resume — 2026-09-03 · PREMIUM 3D INTRO / SPLASH SYSTEM (v174)

## আগের অবস্থা (inspect)
- IndexedDB boot-এর আগে `#app > .app-loading` static (🥄 cube spinner + "Preparing your study space…"), `renderStartupLoading()`-এ same spinner, `phase1-upgrade.js`-এ `#phase1-splash` fixed z-9999 same simple. ৩ জায়গায় ৩ রকম low-quality splash।

## নতুন সিস্টেম `splash-3d.js` (ভাই-স্পেক অনুযায়ী)
- **Central hero:** emerald rounded-square 3D tile (CSS gradient + inset highlight + reflection shine + drop glow) + white 4-point sparkle SVG
- **6 floating 3D objects** (graduation cap, target+arrow, chart card, checklist, book, learning-network) — সব inline SVG, আলাদা depth/delay, আলাদা float speed, কখনো jump নয়
- **Orbit system:** ২টা glowing ring (একটা dashed) + 5 luminous nodes, 26s/19s ঘোরে; subtle dust particles
- **3D platform:** ৩-স্তর emerald ellipse pedestal + soft glow shadow
- **Background:** #f1faf6-family radial ambient glow + light bloom (existing mint বজায়)
- **Copy:** Admission Hub (strong title, em gradient) + বাংলা tagline `তোমার সম্পূর্ণ ভর্তি প্রস্তুতির নির্ভরযোগ্য সঙ্গী।`
- **Loading:** elegant track + emerald bar; **আসল init-এর সাথে sync** — boot(): openDB→"তোমার ডেটা সুরক্ষিতভাবে লোড হচ্ছে…" (45%), seedIfEmpty→"তোমার বিষয় ও অগ্রগতি প্রস্তুত হচ্ছে…" (80%), ready→"প্রায় প্রস্তুত…" (100%) → dismiss(520ms) cross-fade (scale .97 + opacity)
- **Timeline:** 0s bg → .2 glow → .3 platform rise → .5 tile → .72–1.12 objects pop (stagger) → .9 orbit → 1.15 title → 1.32 tagline → 1.5 loader; 2.2s-পর idle floating loop (transform-only)
- **Motion quality:** শুধু transform/opacity (GPU), `will-change` hero/objects, spring-ish cubic-bezier; কোনো spin/bounce/flash নয়
- **Reduced motion:** `prefers-reduced-motion` → সব animation band, শুধু fade; JS detect → `.ahfs-reduced`
- **Performance:** dismiss-এর পর scene DOM থেকে সরিয়ে `animation:none` — dashboard-এ কোনো GPU load নেই; prefill প্রথম-paint fallback (JS লাগার আগেও ব্র্যান্ড দেখা যায়)
- **NO DEMO DATA:** শুধু brand + tagline; স্প্ল্যাশ-এ কোনো ইউনিভার্সিটি/ইউনিট/বিষয় নেই (test-এ assert করা)
- **যেখানে ব্যবহার:** `#app` inline splash + `renderStartupLoading()` + `phase1-upgrade.js #phase1-splash` (সব একই system; urgent-fix route-loading text-ও বাংলা ব্র্যান্ড-ভাষায়) — কোনো ভিন্ন low-quality intro বাকি নেই
- **Auth flow অপরিবর্তিত:** splash overlays শুধু ভিজ্যুয়াল; auth gate/onboarding/dashboard logic স্পর্শ করা হয়নি; logged-out-এ intro→auth, logged-in-এ intro→dashboard (boss-স্পেক flow-ই)

## টেস্ট
- jsdom রান-টেস্ট `splash.test.mjs` — ১৬/১৬ ✅ (mount, tile, spark, ৬ obj, ২ ring, platform, title, বাংলা tagline/stage, NO-demo-data, setStage, setProgress, dismiss cleanup)
- `node --check splash-3d.js` ✅ · AUTH guard demo+hub PASS (regex-এ splash3d-ট্যাগ + `-?` ফরম্যাট) ✅
- sw `v174-splash3d-20260903` (index+sw sync ✓), splash-3d.js `?v=splash3d-v1` APP_SHELL-এ

## স্থিতি
commit+push (demo+hub) → Pages live verify। প্রিভিউ: workspace `splash-preview.html` (self-contained)।
