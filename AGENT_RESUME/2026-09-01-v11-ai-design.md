# v11 — AI-বানানো প্রিমিয়াম ডিজাইন বসানো (ইউজার HTML)

তারিখ: 2026-09-01 · commit ট্যাগ: v11-ai-design

## যা হলো
- ভাই AI দিয়ে ১০-স্ক্রিন প্রিমিয়াম ডিজাইন-কোড বানিয়েছেন (`uploads/index 11.html` + রেফারেন্স ছবি IMG_3258/3299) — সেটা অ্যাপে বসানো হয়েছে।
- **onboarding.css** — AI HTML-এর `<style>` হুবহু (আসল ডিজাইন): #1c7f45 সবুজ, 16px rounded, সফট শ্যাডো, fadeIn, gauge/ring/plan অ্যানিমেশন। Gate-এ wrap: `#ahOnboardGate` fixed + `.app-shell` max-430px, ফোন-মকআপ ফিল।
- **onboarding.js** — সম্পূর্ণ নতুন (AI HTML-এর স্ট্রাকচার), কিন্তু **অ্যাপ API অক্ষত**: `window.AHOnboard = { maybeStart, start }`, per-user persist (localStorage + worker PUT), resume, finish → navigate('dashboard')।
- ১০ স্ক্রিন: s0 Welcome → s1 Goal → s2 University (search + ৩-কলাম লোগো গ্রিড) → s3 Unit → s4 Study Goal → s5 Gauge+chips → s6 Habit (session+days) → s7 Almost (tap/2.5s auto) → s8 Building (০→১০০% auto→dash) → s9 Ready Dashboard (greet, goal card, focus, ring)।
- **আসল লোগো বসানো** (ভাইয়ের নির্দেশ): du.png (ঢাকা), ju.png (জাহাঙ্গীরনগর), ru.png (রাজশাহী), cu.png (চট্টগ্রাম) → uni-grid + confirm + unit-hero-এ। KU/BAU-র অফিসিয়াল লোগো আমাদের কাছে নেই → ডিফল্ট সবুজ-ক্যাপ SVG (AI-ফেক লোগো নয়)।
- গেজ ট্যাপ/ড্র্যাগে বদলযোগ্য (ছোট সংযোজন — HTML-এ শুধু অ্যানিমেশন ছিল); ring % = weeklyDays/7 (ডাইনামিক)।
- jsdom টেস্ট (test-onboarding-v11.js) — S1→S10 + back + finish→dashboard + resume-skip পাস ✅

## ফাইল
- onboarding.css (নতুন), onboarding.js (নতুন), index.html (`?v=p6-onboard-v11`), AGENT_RESUME/2026-09-01-v11-ai-design.md
- data shape: goal(uni/hsc/ssc/bcs) · targetUniversityIds(du/ju/ru/cu/ku/bau) · targetUnits(1) · studyGoal(top/good/fast) · currentLevel(0-100) · weakSubjects · session(10-20/30-60/1-2h/flex) · weeklyDays(3-7)
