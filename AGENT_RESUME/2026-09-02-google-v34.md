# Resume — 2026-09-02 · Google resilience v34 — LIVE

## ভাইয়ের রিপোর্ট (স্ক্রিনশট IMG_3368 + IMG_3371, OCR)
- verify স্ক্রিন নতুন copy (v33-এ আছে — SW আপডেট কাজ করেছে)
- **"Continue with Google" → "Google login is not set up"** — cfg.googleClientId খালি = `/api/auth/config` fetch fail (নেটওয়ার্ক/edge)
- "gmail verification এর নিচে passkey verification টাও নাই" — v33-এ verify-তে passkey সরানো হয়েছে (ভাই লক্ষ্য করেছেন)
- "gmail verification করতে গেলে আগে লগইন করো" — পুরনো verify-passkey বাটনের বাগ (v33-এ বন্ধ); pkRegFinish worker-এ auth লাগে না (route authUser-এর আগে)

## Root cause
- `boot()`-এ config fetch fail হলে `cfg` null → গুগল বাটন "Google login is not set up" (retry ছিল না)
- Browser/worker cache-এ পুরনো config (heuristic cache) সম্ভব

## ফিক্স v34 (commit `0d37efa` — LIVE)
1. **boot()**: config fetch fail → ৩ বার retry (৭০০/১৪০০/২১০০ms) — fail তবেই offline note
2. **doGoogle()**: cfg.googleClientId খালি থাকলে **on-demand refetch** `/api/auth/config` → সফল হলে গুগল চলে; fail-এ স্পষ্ট বাংলা "ইন্টারনেট সংযোগ দেখে একটু পরে আবার চাপো, নয়তো ইমেইল/পাসওয়ার্ড" (আর "not set up" নয়)
3. **api()**: AbortController ১৫s timeout → নেটওয়ার্ক hang-এ "ইন্টারনেট সংযোগ নেই — আবার চেষ্টা করো"
4. **worker deploy** (`08cae72e`): সব JSON response-এ `Cache-Control: no-store` — ভাইয়ের ব্রাউজার কখনো পুরনো config পাবে না
5. SW BUILD_ID `v164-auth-fix-20260902`, APP_SHELL `p3-auth-prof-v34`

## টেস্ট (jsdom /tmp/t/test2.js) — ২/২ ✅
config ২ বার fail → boot retry → ৩য় বার সফল; login-এ গুগল ক্লিক → "লোড হচ্ছে না" নয় → startGooglePicker চলে

## Live verify
- index `p3-auth-prof-v34` + sw `v164` (Pages build ~১৫s)
- live JS: boot retry ✓, on-demand refetch ✓, AbortController ✓, "not set up" মেসেজ ০ ✓
- worker: `cache-control: no-store` ✓, config googleClientId ✓, register-email OTP `sent:true` ✓

## Git
- demo HEAD `0d37efa` (rebase-এ conflict → --theirs) pushed
- deploy master `/home/user/ah-deploy/gk-agent-worker.js` = deployed `08cae72e`

## পরের ধাপ
- resume commit+push (demo+control), deploy master sync
- ভাইকে: রিফ্রেশ → গুগল চাপুন (আর "not set up" আসবে না; নেটওয়ার্ক দুর্বল হলে ৩ বার retry + চাপলে আবার চেষ্টা) → Google window-এ Advanced→Continue ধাপ
- কনফার্মেশন; fail স্ক্রিনশট এলে screen-specific fix
