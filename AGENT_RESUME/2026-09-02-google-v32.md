# Resume — 2026-09-02 · Google login v32 (সফল লাইভ)

## ভাইয়ের রিপোর্ট
"গুগল দিয়ে লগইন করা যাচ্ছে না কেনো 😡🥹" — আগের ধাপের পরও ভাই গুগলে ঢুকতে পারছেন না।

## Root cause বিশ্লেষণ
- কোড রিভিউ (premium-auth.js + worker `authGoogle`) — server/client logic **ঠিক**।
- আসল কারণ সম্ভবত **ব্রাউজার/পপ-আপ লেভেল**: (a) Safari-তে GIS পপ-আপ gesture-সংবেদনশীল — ক্লিক থেকে async `loadGis()`-এর গ্যাপে gesture হারিয়ে পপ-আপ ব্লক; (b) গুগলের unverified-app সতর্কবার্তা দেখে ভাই বন্ধ করে দিচ্ছেন → `popup_closed_by_user`।

## ফিক্স v32 (commit `348cdbe` live)
1. **index.html**: `gsi/client` `async` → **`defer`** (document-order: GIS আগে execute → premium-auth load-এ google available → ক্লিকের মূহূর্তেই sync `requestAccessToken` → Safari gesture preserved, popup ঠিক খোলে)।
2. **premium-auth.js** `startGooglePicker`: error-map-এ স্পষ্ট ধাপ — "অ্যাকাউন্ট বাছাই → Continue; সতর্কবার্তা দেখালে **Advanced → Continue to (unsafe)**"।
3. **Fallback বাটন**: গুগল fail হলে error-এর নিচে `.ah-g-help` (ধাপ) + `#ahEmailFallback` বাটন "📧 ইমেইল দিয়ে ঢুকো" → `go('login')` (ইমেইল+পাসওয়ার্ড পথ)।
4. **CSS**: `.ah-g-help`, `.ah-g-fb` যোগ (premium-auth.css)।
5. **Version**: SW BUILD_ID `v162-auth-fix-20260902`, APP_SHELL `p3-auth-prof-v32` (index ২ রেফ, sw ৩ রেফ, expectedSwVersion+register `v162`), tip text আপডেট (login+signup)।

## টেস্ট (jsdom, /tmp/t)
- login render → google click → GIS stub `popup_closed_by_user` → বাংলা error "বন্ধ" ✓, `.ah-g-help` ✓, fallback বাটন ✓ → ক্লিকে email form (`ahPass`) ✓।

## Live verify
- index `p3-auth-prof-v32` + `gsi/client defer` ✓ (Pages build ~৩০s)
- sw.js `v162-auth-fix-20260902` + APP_SHELL v32 ✓
- premium-auth.js live-তে `ahEmailFallback` ৫ রেফ ✓

## Git
- demo HEAD `348cdbe` (v32) — remote repair: fetch + `git checkout --theirs` (conflict ৪ ফাইল) + rebase --continue + push; clean.
- resume push বাকি: commit এই ফাইল + LATEST.md + push।

## পরের ধাপ
- LATEST.md আপডেট + push (demo + resume) + control sync।
- ভাইকে বলতে হবে: (১) সাইট খুলে ২ সেকেন্ড পর রিফ্রেশ (v32 আসবে), (২) গুগলে Advanced→Continue ধাপ, (৩) fail হলে নতুন "ইমেইল দিয়ে ঢুকো" বাটন, (৪) error-এর লেখা/স্ক্রিনশট এলে screen-specific fix।
