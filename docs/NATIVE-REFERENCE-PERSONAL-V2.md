# Native reference Personal v2 — retired

Status: **retired and removed** in `v240-code-native-entry-20260911`.

Although v2 used HTML/CSS shapes instead of a raster crop, the user correctly rejected the result because the combined illustration still looked like one pasted image and the spacing/positioning did not feel like a real premium interface.

The profile tile, pedestal, book, cap, plane, leaf, orbit, sparkle, landscape, and their old CSS were removed. They must not return. The active input-bound replacement is documented in `CODE-NATIVE-ENTRY-V1.md`.
