# Static Reference Welcome v3 — retired

Status: **retired and removed** in `v240-code-native-entry-20260911`.

The user rejected the image-led Welcome composition because it still looked like a picture placed on a page, had weak positioning, and left unhelpful side space. Its `onboarding-welcome-hero.webp` asset, runtime reference, service-worker entry, bundle expectation, and publication fetch were deleted.

It must not return. The active replacement contract is documented in `CODE-NATIVE-ENTRY-V1.md`.
